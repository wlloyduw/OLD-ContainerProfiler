
config_read_file() {
    (grep -E "^${2}=" -m 1 "${1}" 2>/dev/null) | head -n 1 | cut -d '=' -f 2-;
}

config_get() {
    temp="${1%\"}"
    temp="${temp#\"}"
    val="$(config_read_file ~/.config/graphing_tool/graph_generation_config.cfg "${temp}")";
    printf -- "%s" "${val}";
}
